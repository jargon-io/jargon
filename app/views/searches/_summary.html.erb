<%# locals: (search:) %>
<div class="space-y-4">
  <% if search.summary.present? %>
    <p class="text-lg leading-relaxed"><%= render_content(search.summary, current_item: search) %></p>
  <% end %>
  <%= render "shared/snippet", text: search.snippet, current_item: search %>
</div>
